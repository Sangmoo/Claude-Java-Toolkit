━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🤖 Claude Java Toolkit {{ .Chart.AppVersion }} 이 배포되었습니다.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

릴리스 이름:  {{ .Release.Name }}
네임스페이스: {{ .Release.Namespace }}
DB 타입:     {{ .Values.db.type }}
복제 수:      {{ .Values.replicaCount }}{{ if .Values.autoscaling.enabled }} (HPA 활성)
{{- end }}

▣ 접근 방법:
{{- if .Values.ingress.enabled }}
   브라우저에서 http://{{ .Values.ingress.host }} 접속 (Ingress)
{{- else }}
   포트 포워드:
     kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "claude-toolkit.fullname" . }} 8027:{{ .Values.service.port }}
   접속: http://localhost:8027
{{- end }}

▣ 초기 계정:  admin / admin1234  (반드시 첫 로그인 후 비밀번호 변경)

▣ 헬스체크:
   kubectl exec -n {{ .Release.Namespace }} \
     deploy/{{ include "claude-toolkit.fullname" . }} -- \
     curl -s http://localhost:{{ .Values.service.targetPort }}/actuator/health

▣ 로그 확인:
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} -f

{{- if eq .Values.claude.apiKey "" }}
{{- if eq .Values.secret.existingSecret "" }}

⚠️  CLAUDE_API_KEY 가 비어있습니다!
   다음 명령으로 설정해주세요:
     kubectl create secret generic {{ include "claude-toolkit.fullname" . }}-claude \
       --from-literal=CLAUDE_API_KEY=sk-ant-... \
       -n {{ .Release.Namespace }} --dry-run=client -o yaml | kubectl apply -f -
   또는 helm upgrade --set claude.apiKey=sk-ant-...
{{- end }}
{{- end }}

문서: https://github.com/Sangmoo/Claude-Java-Toolkit
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

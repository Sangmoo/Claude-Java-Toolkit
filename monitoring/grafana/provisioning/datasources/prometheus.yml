# v4.3.0 — Grafana 가 컨테이너 시작 시점에 자동으로 등록할 데이터소스.
# 같은 docker-compose 네트워크 안의 prometheus 서비스를 참조.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s

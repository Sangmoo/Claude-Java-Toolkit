package io.github.claudetoolkit.ui.controller;

import org.springframework.stereotype.Controller;

/**
 * Home page controller — page rendering removed after React SPA migration.
 * Home page data is now fetched by React via API calls.
 * SpaViewResolver handles routing to index.html.
 */
@Controller
public class HomeController {
}
